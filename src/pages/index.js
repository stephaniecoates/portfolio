import React from 'react';
import Layout from './../components/layout'

export default function Home (props) {
    return (
        <Layout title={`SC`} location={props.location}><div>Home</div></Layout>
        
    )
}