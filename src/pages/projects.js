import React from 'react';
import Layout from './../components/layout'

export default function Projects (props) {
    return (
        <Layout title={`SC`} location={props.location}><div>Projects</div></Layout>
        
    )
}