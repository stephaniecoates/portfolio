import React from 'react';
import Layout from './../components/layout'

export default function Contact (props) {
    return (
        <Layout title={`SC`} location={props.location}><div>Contact</div></Layout>
        
    )
}